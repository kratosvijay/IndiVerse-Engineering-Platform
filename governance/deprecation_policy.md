# Deprecation Policy

Standards for retiring legacy patterns or dependencies:

- **Notice**: Mark old rules or packages as `Deprecated` in `ARCHITECTURE_VERSION.md`.
- **Support Window**: Maintain compatibility for exactly one major release lifecycle.
- **Sunset**: Remove codebase patterns once compatibility window expires.
